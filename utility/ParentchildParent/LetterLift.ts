import React, { useState, useEffect, useRef } from 'react';

type AnimProps = {
    speed: number,
    children: JSX.Element
}

const LetterLife = ({ speed, children } : AnimProps ) => {

}
